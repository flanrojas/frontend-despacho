services:
  front_despacho:
    build:
      context: .
      args:
        VITE_VENTAS_API_URL: ${VITE_VENTAS_API_URL}
        VITE_DESPACHOS_API_URL: ${VITE_DESPACHOS_API_URL}
    ports:
      - "3000:80"
